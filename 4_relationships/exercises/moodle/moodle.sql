drop database if exists jp23_3_moodle;
create database jp23_3_moodle;

use jp23_3_moodle;

create table assign(
   id          bigint(10) primary key not null auto_increment,
   course      bigint(10) primary key not 
);
